# Labor solo DOCX PoC

Сгенерировать тестовый DOCX вручную:

```bash
node scripts/render-labor-solo-docx-poc.mjs
```

Результат появится в:

```text
dev-output/labor-solo-poc.docx
```

Скрипт не импортируется приложением и не подключён к пользовательскому интерфейсу.

